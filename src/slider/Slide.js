import React from 'react'

const Slide = ({  }) => {
  const styles = {
    backgroundColor: '#fff',
    backgroundSize: 'cover',
    backgroundRepeat: 'no-repeat',
    backgroundPosition: '50% 60%'
  }
  return <div className="slide" style={styles}>H</div>
}

export default Slide